
## ✅ [결과] 15:10:00
- **수정 파일**: `js/projects.js`, `js/modal.js`, `css/modal.css`, `videos/`(신규 폴더)
- **변경 내용**:
  - `videos/`(신규): `images/`, `pdfs/`와 동일하게 프로젝트 루트에 비-이미지 에셋 폴더 신설(현재 빈 폴더, 사용자가 `guider_demo_1.mp4`/`guider_demo_2.mp4`를 직접 저장할 위치)
  - `js/projects.js`: Guider(id: 13) `detailData`에 `demoVideos: ["videos/guider_demo_1.mp4", "videos/guider_demo_2.mp4"]` 추가 (`details`와 `outcome` 사이)
  - `js/modal.js`: `openProjectDetailModal()`에 "Demo Video Section" 렌더링 블록 추가 — `data.demoVideos` 배열을 `<video autoplay muted loop playsinline controls>` 그리드로 렌더링(Outcome 섹션 바로 위에 배치)
  - `css/modal.css`: `.detail-video-grid`(2열 grid)/`.detail-video`(rounded, 검정 배경) 스타일 추가, 768px 이하에서 1열로 전환되는 반응형 규칙 추가
- **이유**: Guider 상세 모달에 시연 영상 2개를 넣어달라는 요청 반영. 실제 GIF 변환 대신 `autoplay+muted+loop` 비디오로 GIF와 동일한 시각 효과를 내되 용량/화질에서 유리한 방식 채택. Playwright로 데스크톱(900px) 뷰포트에서 "시연 영상" 섹션이 2열 그리드로 정상 렌더링되는 것을 확인(실제 mp4 파일은 아직 사용자가 추가 예정이라 빈 플레이어로 표시됨)

## ✅ [결과] 15:45:00
- **첨부 이미지 요약**: 히어로 섹션의 React / Spring Boot / AWS 기술 스택 뱃지 3개 스크린샷
- **수정 파일**: `index.html`, `css/hero.css`, `css/responsive.css`, `js/animation.js`
- **변경 내용**:
  - `index.html`: 히어로 섹션의 `.hero-stack`(React/Spring Boot/AWS pill 3개) 마크업 전체 삭제
  - `css/hero.css`: 이제 쓰이지 않는 `.hero-stack`/`.stack-item`/`.stack-item:hover`/`.stack-item .fa-leaf` 규칙 삭제
  - `css/responsive.css`: 480px 이하 `.hero-stack`/`.stack-item` 축소 규칙 삭제
  - `js/animation.js`: `.stack-item`에만 쓰이던 `initStackItemEffects()` 함수와 `initAllAnimations()` 내 호출 삭제(마크업 제거로 완전히 죽은 코드가 되어 함께 정리)
- **이유**: 히어로 섹션에서 기술 스택 뱃지를 제거해달라는 요청 반영. 관련 CSS/JS가 다른 곳에서 재사용되지 않는 것을 확인 후 함께 정리. Playwright 스크린샷으로 뱃지가 사라지고 나머지 레이아웃(수상/논문/특허 하이라이트, 이력서 다운로드 버튼)은 정상 유지됨을 확인
