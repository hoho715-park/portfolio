/* ===================================
   Fullpage Scroll JS - Disabled
   =================================== */

/**
 * 풀페이지 스크롤 기능 비활성화
 * - 브라우저 기본 스크롤 사용
 * - 레이아웃 잘림 방지
 * - 반응형 안정성 확보
 */

function initFullpageScroll() {
  return;
}

window.initFullpageScroll = initFullpageScroll;
window.getFullpageScroll = () => null;
